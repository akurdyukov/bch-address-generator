Simple BitcoinCash (BCH) address generator

Usage:
```
➜  bch-address-generator npm start -- -h

> bch-address-generator@1.0.0 start /Users/alik/Development/unitedtraders/bch-address-generator
> node index.js "-h"

Usage: index [options]

Options:
  -c, --count <number>     Number of keys to generate (default: 1)
  -n, --network <network>  Network to use: live or test (default: "live")
  -h, --help               output usage information
```
